<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mobile_1_23227_ca1">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Mobile123227ca1">
        <activity android:name=".IrishActivity"></activity>
        <activity android:name=".EarlActivity" />
        <activity android:name=".LapsangActivity" />
        <activity android:name=".AssamActivity" />
        <activity android:name=".DbActivity" />
        <activity android:name=".CartActivity" />
        <activity android:name=".DarjellingActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>